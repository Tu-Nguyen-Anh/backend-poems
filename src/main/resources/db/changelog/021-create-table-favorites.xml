<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">

  <!-- Bài thơ yêu thích của người dùng (mỗi cặp user–poem chỉ 1 dòng). -->
  <changeSet id="021-create-favorites" author="claude">
    <createTable tableName="favorites">
      <column name="id" type="BIGSERIAL">
        <constraints primaryKey="true" nullable="false"/>
      </column>

      <column name="user_id" type="BIGINT">
        <constraints nullable="false"
                     foreignKeyName="fk_favorites_user"
                     references="users(id)"/>
      </column>

      <column name="poem_id" type="BIGINT">
        <constraints nullable="false"
                     foreignKeyName="fk_favorites_poem"
                     references="poems(id)"/>
      </column>

      <column name="is_deleted" type="BOOLEAN" defaultValueBoolean="false"/>
      <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP"/>
      <column name="updated_at" type="TIMESTAMP WITH TIME ZONE"/>
    </createTable>

    <addUniqueConstraint tableName="favorites"
                         columnNames="user_id, poem_id"
                         constraintName="uq_favorites_user_poem"/>
    <createIndex tableName="favorites" indexName="idx_favorites_user">
      <column name="user_id"/>
    </createIndex>
  </changeSet>

</databaseChangeLog>
